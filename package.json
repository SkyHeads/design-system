{
  "name": "turborepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@10.4.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.6"
  }
}
